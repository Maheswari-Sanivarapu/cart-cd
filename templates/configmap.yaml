apiVersion: v1 
kind: configMap 
metadata:
  name: cart
  namespace: roboshop
  labels:
    component: cart
    project: roboshop
    tier: app
data:
  REDIS_HOST: redis 
  CATALOGUE_HOST: catalogue
  CATALOGUE_PORT: 8080